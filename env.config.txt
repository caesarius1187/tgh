# Configuración de variables de entorno
# Copia este archivo como .env.local y ajusta los valores

# URL pública de la aplicación
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui
JWT_EXPIRES_IN=7d

# PostgreSQL / Supabase (remoto o local)
POSTGRES_URL=postgres://usuario:password@host:puerto/base?sslmode=require
POSTGRES_URL_NON_POOLING=postgres://usuario:password@host:puerto/base
POSTGRES_PRISMA_URL=postgres://usuario:password@host:puerto/base?pgbouncer=true
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password
POSTGRES_DATABASE=postgres
POSTGRES_SSL=false
POSTGRES_SSL_REJECT_UNAUTHORIZED=false

# Supabase API (si usas Supabase)
SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=anon_key_aqui
SUPABASE_SERVICE_ROLE_KEY=service_role_key_aqui
SUPABASE_JWT_SECRET=supabase_jwt_secret_aqui

# Subidas locales
UPLOAD_DIR=./public/uploads
MAX_FILE_SIZE=5242880
